import { Typography } from "@mui/material";
import React from "react";
import { PillComponent } from "./types";

const pillComponent: PillComponent = ({ event }) => {
  return <Typography>Pill</Typography>;
};

export default pillComponent;
