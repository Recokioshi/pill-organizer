import { Typography } from "@mui/material";
import React from "react";
import { MealComponent } from "./types";

const mealComponent: MealComponent = ({ event }) => {
  return <Typography>Meal</Typography>;
};

export default mealComponent;
